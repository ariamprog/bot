# 💙 مشروع دروب آمنة (Duroob Aminah) 🛡️

<img src="https://github.com/user-attachments/assets/a05e6528-9d12-4d7a-b690-ae309dc06d2a" alt="لوقو دروب آمنه" width="180"/>

**مساعد إرشادي ذكي لـ الكلية التقنية الرقمية بجدة.**

---

## 📝 وصف المشروع

**دروب آمنة** هو نظام تفاعلي يعمل كمرشد آلي لمساعدة الطالبات على التنقل والحصول على معلومات فورية داخل مبنى الكلية.

### الميزات الأساسية:
* 🧭 **التنقل الموجه:** توفير مسارات ومعلومات عن المواقع الرئيسية (المكتبة، العيادة، الكافتيريا، مكاتب الإدارة).
* 🚪 **تفاصيل المرافق:** معلومات دقيقة عن البوابات والمواقف والمصاعد.
* 📚 **كشف القاعات:** نظام تحليل لرموز القاعات (مثال: `2-A-01`) لتحديد موقعها فورًا.
* 🤖 **دعم Gemini AI:** استخدام نموذج الذكاء الاصطناعي للإجابة على الاستفسارات العامة والمعقدة.
* 📲 **تواصل مباشر:** زر مباشر للتواصل عبر واتساب للدعم.

---

## 🛠 المتطلبات وهيكلية الملفات

1. **لغة البرمجة**: Python 3.11 أو أعلى  

2. **المكتبات الأساسية**:

   - `gradio` - لإنشاء واجهة المستخدم الرسومية للويب/تطبيق المساعد.  

   - `google-generativeai python-dotenv` - للتفاعل مع نموذج Gemini.

   - `re` و `string` و `difflib` و `datetime` - للمعالجة النصية والتحقق من المدخلات.  

3. **بيئة التشغيل**:  

   - Windows / Mac / Linux  

   - يمكن تشغيله محليًا عبر Python أو رفعه على سيرفر ويب لدعم الوصول العام.  

4. **ملفات المشروع**:  

   - `bot.py` → الكود الرئيسي للبوت.  

   - `README.md` → مستند الشرح هذا.  

5. **ملف المكتبات**:  

   - `requirements.txt` → لتثبيت جميع المكتبات مرة واحدة.



---



## ⚡ تثبيت المكتبات



```bash

pip install -r requirements.txt

pip install gradio

pip install google-generativeai

pip install gradio google-generativeai python-dotenv

pip install pandas numpy python-dotenv  # اختياري



تأكد أن بيئة Python لديك مفعلة وأن الإصدار مناسب (>= 3.11).



## 🔑 إعداد المفتاح السري (Gemini API Key)
لحماية مفتاحك السري، يتم استخدام ملف .env لقراءة المفتاح بشكل آمن.

1. إنشاء المفتاح
أنشئ مفتاح API جديد من [Google AI Studio].

قم بإنشاء ملف باسم .env في جذر المشروع.

2. إضافة المفتاح للملف الآمن
انسخ محتويات ملف .env.example إلى ملف .env وقم بتعويض المفتاح:
# ملف .env
GEMINI_API_KEY=المفتاح_الجديد_الخاص_بك_هنا

1
---



## ⚡ تثبيت المكتبات



```bash

pip install -r requirements.txt

pip install gradio

pip install google-generativeai

pip install gradio google-generativeai python-dotenv

pip install pandas numpy python-dotenv  # اختياري



تأكد أن بيئة Python لديك مفعلة وأن الإصدار مناسب (>= 3.11).

1. **لغة البرمجة**: Python 3.11 أو أعلى  

2. **المكتبات الأساسية**:

   - `gradio` - لإنشاء واجهة المستخدم الرسومية للويب/تطبيق المساعد.  

   - `google-generativeai python-dotenv` - للتفاعل مع نموذج Gemini.

   - `re` و `string` و `difflib` و `datetime` - للمعالجة النصية والتحقق من المدخلات.  

3. **بيئة التشغيل**:  

   - Windows / Mac / Linux  

   - يمكن تشغيله محليًا عبر Python أو رفعه على سيرفر ويب لدعم الوصول العام.  

4. **ملفات المشروع**:  

   - `bot.py` → الكود الرئيسي للبوت.  

   - `README.md` → مستند الشرح هذا.  

5. **ملف المكتبات**:  

   - `requirements.txt` → لتثبيت جميع المكتبات مرة واحدة.



---



## ⚡ تثبيت المكتبات



```bash

pip install -r requirements.txt

pip install gradio

pip install google-generativeai

pip install gradio google-generativeai python-dotenv

pip install pandas numpy python-dotenv  # اختياري



تأكد أن بيئة Python لديك مفعلة وأن الإصدار مناسب (>= 3.11).

---

### 🚀 طريقة تشغيل المشروع


انتقل إلى مجلد المشروع في الطرفية وشغّل البوت:


# 1. الانتقال إلى المجلد
cd /path/to/your/project/folder

# 2. تشغيل البوت
python bot.py
بعد التشغيل، انسخ العنوان المحلي (مثل http://127.0.0.1:7860/) وافتحه في متصفح الويب لبدء استخدام "دروب آمنة".
